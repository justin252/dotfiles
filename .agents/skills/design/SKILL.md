---
name: design
description: "DEPRECATED: use /propose instead. Redirects to propose skill."
---

**Deprecated.** Use `/propose` instead.

`/propose` handles both new projects and new features. It produces `~/documents/<topic>/rfc.md` and optionally scaffolds `impl.md`.
