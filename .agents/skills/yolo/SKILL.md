---
name: yolo
description: "DEPRECATED: use '/implement' with autonomous mode instead."
---

**Deprecated.** Use `/implement` with autonomous mode instead.

`/implement` in autonomous mode does the same thing -- executes all phases, drafts PRs, never merges, stops on failure. It also tracks progress in `impl.md` and supports resume.
