---
name: proposal-template
description: "DEPRECATED: use /propose instead. Redirects to propose skill."
---

**Deprecated.** Use `/propose` instead.

`/propose` produces `~/documents/<topic>/rfc.md` with the same cone-pattern structure, plus integration with `/implement` for phased execution.
